model:
  in_channels: 4
  out_channels: 4
  base_filters: 48
  depth: 5
  kernel_size: 3
  padding: 1
  learning_rate: 0.00034927597265743535
dataset:
  batch_size: 8
  num_workers: 4
  val_split: 0.2
  data_file: ./data/mixer_256.npz
  max_samples: 0
  normalize: true
trainer:
  max_epochs: 20
  accelerator: mps
  log_every_n_steps: 10
experiment_name: default_experiment
