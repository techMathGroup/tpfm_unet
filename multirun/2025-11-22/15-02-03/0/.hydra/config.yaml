model:
  in_channels: 4
  out_channels: 4
  base_filters: 64
  depth: 3
  kernel_size: 3
  padding: 1
  learning_rate: 0.0008404702680216842
dataset:
  batch_size: 8
  num_workers: 4
  val_split: 0.2
  data_file: ./data/mixer_256.npz
  max_samples: 0
  normalize: true
trainer:
  max_epochs: 20
  accelerator: auto
  log_every_n_steps: 10
experiment_name: default_experiment
